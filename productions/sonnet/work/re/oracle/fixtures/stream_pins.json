{
  "_comment": [
    "The shared-LCG boundary states the project has pinned, promoted from",
    "prose into a fixture.  status: 'emulator' = reproduced by executing the",
    "original under re/oracle (proof — an LCG state has no near-misses);",
    "'port' = verified in the JS port / independent derivation, not yet",
    "emulator-confirmed.  test/stream_trace_test.mjs asserts the port against",
    "these on its path.  2026-08-12: the whole scene-0..2 build chain is now",
    "emulator-confirmed end to end (fixtures/scenebuild/scenes_0_1_2) — the",
    "historical '512-draw gap' (Codex H3) is CLOSED: 0xb67fd936 was the set-2",
    "bake's post-texgens state and 0x5f95db36 is post-records AND the true",
    "first-cluster pre-yaw state, in the original and the port alike."
  ],
  "lcg": "seed = (seed*214013 + 2531011) & 0xffffffff  — [0x41a9b8]",
  "pins": [
    {"state": "0x71224a04", "status": "emulator",
     "what": "scene-2 build ENTRY (after scenes 0+1 built, from image seed 1)",
     "ref": "fixtures/scenebuild/scenes_0_1_2/trace.json"},
    {"state": "0xb9583054", "status": "emulator",
     "what": "stream entering buildTree at the set-0 impostor bake (first array-C cluster scattered)",
     "ref": "scenebuild trace boundary 5-7"},
    {"state": "0xdedf2c8d", "status": "emulator",
     "what": "after the tree's draws incl. the 32-per-leaf record tail, textures precached (port buildTree lands here)",
     "ref": "re/oracle/fixtures/tree/impostor_set0"},
    {"state": "0xa661ec3b", "status": "emulator",
     "what": "the original's buildTree RETURN state at set 0 = tree + tail + inline texgen 0+1 == entering the per-pass yaws",
     "ref": "scenebuild trace: buildTree.ret; targets/tree.py case B"},
    {"state": "0x577c5291", "status": "emulator",
     "what": "set-0 post-passes == set-1 buildTree entry",
     "ref": "scenebuild trace boundaries 9-10"},
    {"state": "0x3316173d", "status": "emulator",
     "what": "set-1 buildTree return (textures cached, passes draw nothing) == set-2 buildDandelion entry",
     "ref": "scenebuild trace boundaries 11-13"},
    {"state": "0xb67fd936", "status": "port",
     "what": "set-2 post-texgens (after program 3's reseed), BEFORE the 512 record draws — historically mislabelled 'first-cluster pre-yaw', which caused the phantom 512-draw gap",
     "ref": "scene7 __bakeProbe 'set2 post-texgens'"},
    {"state": "0x5f95db36", "status": "emulator",
     "what": "set-2 post-records AND the true first-cluster pre-yaw state (Codex H3's prediction — CONFIRMED, gap closed)",
     "ref": "scenebuild trace: first yawLoop hit; port probe 'set2 post-records'"},
    {"state": "0x5e3e2400", "status": "emulator",
     "what": "array-F plant buildDandelion entry in scene 2 — identical in original and port, ELIMINATING stream position as the cause of the prop head displacement",
     "ref": "scenebuild trace boundary 308; port probe 'arrayF pre-buildDandelion'"},
    {"state": "0xd9d74701", "status": "port",
     "what": "after buildDandelion (4352 geometry draws) in the same-seed A/B",
     "ref": "re/scenes/DANDELIONS.md"}
  ]
}
